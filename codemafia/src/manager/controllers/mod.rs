/* Defines the application's controllers. Controllers are responsible for receiving and processing messages, by issuing
events to the correct dispatcher. */

pub mod internal;
pub mod shared;

/* Utility functions used in more than 1 controller. */
pub mod util;
