use super::GameServer;

/* Game board specific event handling. */
impl GameServer {
    fn send_initial_game_state(&self) {
        /* Send the board to each player, but for special roles, reveal word colours. */
    }
}